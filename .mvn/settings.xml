<settings>
    <mirrors>
        <mirror>
            <!--This sends everything else to /public -->
            <id>nexus</id>
            <mirrorOf>*</mirrorOf>
            <url>https://nexus.codeo.intranet.justice.gouv.fr/repository/composer-proxy/</url>
        </mirror>
    </mirrors>
    <profiles>
        <profile>
            <id>nexus</id>
            <!--Enable snapshots for the built in central repo to direct -->
            <!--all requests to nexus via the mirror -->
            <repositories>
                <repository>
                    <id>central</id>
                    <url>https://nexus.codeo.intranet.justice.gouv.fr/repository/composer-proxy/</url>
                    <releases><enabled>true</enabled></releases>
                    <snapshots><enabled>true</enabled></snapshots>
                </repository>
            </repositories>
        </profile>
        <profile>
            <id>sonar</id>
            <activation>
                <activeByDefault>true</activeByDefault>
            </activation>
            <properties>
                <sonar.host.url>
                    https://sonar-bravo.codeo.intranet.justice.gouv.fr
                </sonar.host.url>
                <sonar.verbose>true</sonar.verbose>
                <sonar.showProfiling>true</sonar.showProfiling>
                <sonar.login>a1fa76199695deac22630f9a2d240b7106b035ee</sonar.login>
                <sonar.sourceEncoding>UTF-8</sonar.sourceEncoding>
            </properties>
        </profile>

    </profiles>
    <servers>
        <server>
            <id>central</id>
            <username>anj-ci1</username>
            <password>ProtoconchalFurlana</password>
        </server>
    </servers>
    <activeProfiles>
        <activeProfile>nexus</activeProfile>
    </activeProfiles>
</settings>
